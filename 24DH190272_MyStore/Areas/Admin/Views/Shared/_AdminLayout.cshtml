<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Trang Quản Trị</title>

    @Styles.Render("~/Content/css")

    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/Site.css" rel="stylesheet" />
    <link href="~/Content/StyleCSS/AdminLayoutSheet.css" rel="stylesheet" />
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    @* Áp dụng class Cam Nhạt *@
    <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-custom-light-orange">
        <div class="container">
            @* Logo *@
            @Html.ActionLink("MY STORE ADMIN", "Index", "Home", new { area = "Admin" }, new { @class = "navbar-brand" })

            @* QUAN TRỌNG: Thêm nút Toggler lại (dù bạn không dùng mobile) để giữ cấu trúc Bootstrap *@
            <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" title="Toggle navigation" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            @* SỬA LỖI: Thêm justify-content-between để đẩy Đăng xuất sang phải *@
            <div class="collapse navbar-collapse d-sm-inline-flex justify-content-between">

                @* BÊN TRÁI: Các mục điều hướng chính *@
                @* Dùng mr-auto để đẩy các mục còn lại sang phải *@@* ms-3 để cách logo *@
                <ul class="navbar-nav mr-auto ms-3">
                    <li class="nav-item">
                        @Html.ActionLink("Danh Mục", "Index", "Categories", new { area = "Admin" }, new { @class = "nav-link" })
                    </li>

                    <li class="nav-item">
                        @Html.ActionLink("Sản phẩm", "Index", "Products", new { area = "Admin" }, new { @class = "nav-link" })
                    </li>
                </ul>

                @* BÊN PHẢI: Mục Đăng xuất *@
                @* KHÔNG CẦN ms-auto ở đây vì justify-content-between đã làm nhiệm vụ này *@
                <ul class="navbar-nav">
                    <li class="nav-item">
                        @Html.ActionLink("Đăng xuất", "Logout", "Account", new { area = "" }, new { @class = "nav-link nav-logout" })
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Trang Quản Trị</p>
            <p>Nhóm phát triển W06-15</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>